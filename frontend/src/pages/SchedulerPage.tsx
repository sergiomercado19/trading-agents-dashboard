export default function SchedulerPage() {
  return (
    <div>
      <h2 style={{ fontSize: 18, marginBottom: 16 }}>Scheduler</h2>
      <p style={{ color: "var(--text-muted)" }}>
        Manage cron-based recurring analysis jobs.
      </p>
      <div
        style={{
          marginTop: 24,
          padding: 16,
          border: "1px solid var(--border)",
          borderRadius: 8,
          background: "var(--bg-secondary)",
        }}
      >
        <p style={{ color: "var(--text-muted)", fontSize: 13 }}>
          Scheduler will be implemented in Milestone 3.
        </p>
      </div>
    </div>
  );
}
