export default function ChatPage() {
  return (
    <div>
      <h2 style={{ fontSize: 18, marginBottom: 16 }}>Chat</h2>
      <p style={{ color: "var(--text-muted)" }}>
        Chat with AI about your reports, pin context, stream responses.
      </p>
      <div
        style={{
          marginTop: 24,
          padding: 16,
          border: "1px solid var(--border)",
          borderRadius: 8,
          background: "var(--bg-secondary)",
        }}
      >
        <p style={{ color: "var(--text-muted)", fontSize: 13 }}>
          Chat page will be implemented in Milestone 7.
        </p>
      </div>
    </div>
  );
}
